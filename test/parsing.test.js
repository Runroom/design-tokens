'use strict';

require = require("esm")(module /*, options */);
const expect = require('chai').expect;
const figma = require('../src/figma-cli');
const mockJson = require('./data');

describe('Figma cli', () => {
  describe('getColor', () => {
    console.log(mockJson);
  });
});
